# Provider-Data-Project
A pipeline that pulls provider records from the public NPI Registry API, loads them into a relational database, validates them with SQL, exports client-specific CSV/JSON files, and ships with an integration guide.
